/* theme/adapters — curated: yalnız UI token→CSS adaptörü (grid/chart tema
   adaptörleri bilinçle dışlandı — MFE START GATE curated sınırı). */
export { tokenSetToCss, applyTokenSet } from "./ui-adapter";
