package com.ats.interview;

/** Mülakat yaşam döngüsü; başvuru status'u ile bilinçli olarak aynı state machine değildir. */
public enum InterviewStatus {
    SCHEDULED,
    COMPLETED,
    CANCELLED
}
